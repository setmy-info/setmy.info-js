# Dependencies
node_modules/

# Build results: bundles (packages/*/dist, packages/*/web/dist), packed tarballs (dist/)
dist/

# Reports (junit, coverage, security, sbom, dependency tree, docs) - archived by CI, never committed
reports/
coverage/

# Running-instance state (scripts/servers.js) and Deploy stage installations (scripts/deploy.js)
build/

# Logs and caches
*.log
npm-debug.log*
.npm
.eslintcache
*.tsbuildinfo
*.tgz

# IDE
.idea/
.vscode/

# Local only
claude.session.sh
claude.session.cmd
cursor.session.sh
cursor.session.cmd
